year = int(input("which year do you want to check?))
if year % 4 < 1:
    if year % 100 > 0 :
        if year % 400 < 1:
            print("leap.")
        else:
            print("Not leap year.")
    elif year % 400 <1:
        print("Leap year.")
else:
    print("Not a leap year") 
